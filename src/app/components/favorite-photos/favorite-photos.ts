import { ChangeDetectionStrategy, Component, signal } from '@angular/core'; import { PHOTOS } from '../../data/photos.data'; import { PhotoLightbox } from '../photo-lightbox/photo-lightbox';
@Component({selector:'app-favorite-photos',imports:[PhotoLightbox],changeDetection:ChangeDetectionStrategy.OnPush,templateUrl:'./favorite-photos.html',styleUrl:'./favorite-photos.scss'}) export class FavoritePhotos{readonly photos=PHOTOS.filter(p=>p.favorite);readonly selected=signal<number|null>(null);close(){this.selected.set(null);document.body.style.overflow=''}open(i:number){this.selected.set(i);document.body.style.overflow='hidden'}}
